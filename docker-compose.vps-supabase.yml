version: '3.8'

services:
  app:
    image: hariharan11111/focusrobin-production:latest
    container_name: focusrobin_app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
      - PORT=3000
