// PM2 Ecosystem Configuration
// Copy this file to ecosystem.config.js and update the paths

module.exports = {
  apps: [{
    name: 'focusrobin',
    script: 'node .next/standalone/server.js',
    cwd: '/home/your-username/apps/focusrobin',  // UPDATE THIS PATH
    instances: 1,
    exec_mode: 'fork',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    error_file: './logs/pm2-error.log',
    out_file: './logs/pm2-out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
    merge_logs: true,
    autorestart: true,
    max_restarts: 10,
    min_uptime: '10s',
    // Memory limit (adjust based on your VPS)
    max_memory_restart: '1G'
  }]
};























